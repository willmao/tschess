import { ChessInfoPipe } from './chess-info.pipe';

describe('ChessInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ChessInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
